apiVersion: 1

datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    access: proxy
    url: clickhouse:8123
    isDefault: true
    jsonData:
      defaultDatabase: trading
      port: 9000
      server: clickhouse
      username: default
